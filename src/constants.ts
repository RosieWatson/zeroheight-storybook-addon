export const ADDON_ID = "zeroheight-storybook-addon";
export const TAB_ID = `${ADDON_ID}/tab`;
export const KEY = `zeroheight`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
